version: 2

models:
  - name: int_funnel_stages
    description: |
      Cálculo de timestamps clave por etapa:
      enrollment → view → completion → feedback
    columns:
      - name: first_enroll_dt;    tests:[not_null]
      - name: first_view_dt
      - name: first_completion_dt
      - name: first_feedback_dt
